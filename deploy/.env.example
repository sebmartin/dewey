# Copy to deploy/.env and adjust paths for your host.
# Both directories must live on the same filesystem so extensions can
# stage into /tmp and dewey can rename into /data atomically.

DATA_DIR=/srv/dewey/data
TMP_DIR=/srv/dewey/tmp
